#include <stdio.h>
// Reverse a word entered by the user using a for loop

int main() {
    
    char word[100];  // Adjust size as needed
    int length = 0;

    // type your code below
    
    printf("Enter a word (no spaces): ");

    // Read characters using getchar() until newline or EOF

    // Print the reversed word
    
    // type your code above

    return 0;
}


