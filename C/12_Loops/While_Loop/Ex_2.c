#include <stdio.h>

// Count the number of digits in an integer

int main() {
    // type your code below
    int number = 12345; // Test with different positive integer values
    int count = 0;      // Initialize a counter for digits
    
    // Optional: Get input from the user

    // Use a while loop to count the digits
    
    // type your code above
    return 0;
}

// until a certain condition is met, incrementing a counter in each step.